using System;
using System.Web.UI;

// using test_proyect_sistems.Model.Classes;
// using test_proyect_sistems.Model.DataBase;

namespace SistemsProyect.Components.Pages.Actions.Admin.ViewsButton.Teacher.AdminTeacher
{
    public partial class UpdateTeacher : UserControl
    {
        protected void Page_Load(object sender, EventArgs e)
        {
        }

        protected void btnBuscar_Click(object sender, EventArgs e)
        {
            // lblError.Text = "";
            // var criterio = rblBuscarPor.SelectedValue;
            // var email = txtEmail.Text.Trim().ToLower();
            // var phone = txtTelefono.Text.Trim();
            //
            // List<Teacher> results = new List<Teacher>();
            // Teacher teacher = null;
            //
            // if (criterio == "email")
            // {
            //     teacher = TeacherOperations.SearchTeacherEmail(email);
            //     if (teacher == null)
            //     {
            //         lblError.Text = "No se encontró ningún profesor con ese correo.";
            //     }
            // }
            // else // teléfono
            // {
            //     // teacher = TeacherOperations.SearchTeacherPhone(phone);
            //     // if (teacher == null)
            //     // {
            //     //     lblError.Text = "No se encontró ningún profesor con ese teléfono.";
            //     // }
            // }
            //
            // if (teacher != null)
            //     results.Add(teacher);
            //
            // gvResultados.DataSource = results;
            // gvResultados.DataBind();
        }
    }
}